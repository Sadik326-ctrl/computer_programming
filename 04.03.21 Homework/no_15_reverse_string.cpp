#include <bits/stdc++.h>
using namespace std;

string rev(string);

int main()
{
    string word;
    cout<<"Enter the word: ";
    cin>>word;
    cout<<endl<<rev(word);
}

string rev(string word)
{
    if word
}
